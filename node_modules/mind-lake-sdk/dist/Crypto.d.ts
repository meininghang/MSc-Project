import { MindLake } from './MindLake';
import { DataType, ResultType } from './types';
export default class Crypto {
    private service;
    private web3;
    private sdk;
    constructor(sdk: MindLake);
    /**
     * encrypt method
     * @param tableName
     * @param columnName
     * @param data
     * @param schema
     */
    encrypt(data: any, tableNameColumnName: string | DataType): Promise<ResultType>;
    /**
     *
     * @param hex
     */
    decrypt(hex: string): Promise<ResultType>;
    /**
     * get dek
     * @param schema
     * @param table
     * @param column
     * @param walletAddress
     * @private
     */
    private _getOrGenDek;
    private _queryCCEntryByName;
    private _genCCEntryFromLocal;
    private _genSQLInsertDek;
    private _genCryptoHeader;
    private _extractCryptoHeader;
    private _extractEncType;
    private _extractCtxId;
    private _genCheckCode;
}
